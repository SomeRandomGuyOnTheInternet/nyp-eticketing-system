// Database credentials go here 

// These are the database credentials that we use to connect with MySQL
// When creating your MySQL database, make sure everybody using the project configures the database using the same credentials used here
// There's probably a better way of saving these details, as they're in plaintext for the world to see

module.exports = {
    host: "localhost",
    database: "fypjapplication",
    username: "root",
    password: "password"
};